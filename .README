# CMake project generator for Go

## Usage
First compile the source:
```
go build main.go
```

then run:
```
./main -path=/absolute/path/to/project-dir
```

then in your project directory:

```
cmake .
make
```